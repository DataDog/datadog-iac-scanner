apiVersion: v1
kind: Pod
metadata:
  name: plain-root
  namespace: default
spec:
  hostNetwork: true
  hostPID: true
  containers:
  - name: plain
    image: busybox:latest
    securityContext:
      privileged: true
      runAsUser: 0
      allowPrivilegeEscalation: true
