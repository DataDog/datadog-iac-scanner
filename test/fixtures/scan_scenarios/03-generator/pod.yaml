apiVersion: v1
kind: Pod
metadata:
  name: consumer
spec:
  containers:
  - name: consumer
    image: busybox
    envFrom:
    - configMapRef:
        name: app-config
    - secretRef:
        name: api-token
