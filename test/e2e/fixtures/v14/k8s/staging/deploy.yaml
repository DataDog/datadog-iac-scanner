apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-app
spec:
  replicas: 1
  selector:
    matchLabels: { app: staging }
  template:
    metadata:
      labels: { app: staging }
    spec:
      containers:
      - name: app
        image: nginx:latest
        securityContext:
          privileged: true
