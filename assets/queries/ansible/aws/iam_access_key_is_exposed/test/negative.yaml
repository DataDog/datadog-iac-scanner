# Non-root user with access key: not exposed
- name: Create regular user access key
  community.aws.iam_access_key:
    user_name: jcleese
    state: present

# Root user but key is absent: not exposed
- name: Delete root access key
  community.aws.iam_access_key:
    user_name: root
    id: AKIA1EXAMPLE1EXAMPLE
    state: absent
