name: Safe composite github-script
description: Composite action that runs github-script without any user-controlled context
runs:
  using: composite
  steps:
    - name: Safe script
      uses: actions/github-script@v7
      with:
        script: |
          core.info('No user-controlled event data used.')
