name: Composite action with unpinned third-party action
description: Composite action that calls a third-party action by tag instead of SHA
runs:
  using: composite
  steps:
    - name: Set up unpinned tool
      uses: third-party/some-tool@v1
